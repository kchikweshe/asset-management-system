export const purge = [
  './src/**/*.html',
  './src/**/*.js',
];
export const darkMode = false;
export const theme = {
  extend: {},
};
export const variants = {
  extend: {},
};
export const plugins = [require('@tailwindcss/forms')];
